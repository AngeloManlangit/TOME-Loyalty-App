

export function createRemoteJWKSet(): never {
  throw new Error('jose is stubbed in tests — token verification is not exercised here');
}

export function jwtVerify(): never {
  throw new Error('jose is stubbed in tests — token verification is not exercised here');
}

export function decodeProtectedHeader(): never {
  throw new Error('jose is stubbed in tests — token verification is not exercised here');
}

export default {};
