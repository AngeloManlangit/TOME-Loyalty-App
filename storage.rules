rules_version = '2';

// TOME Loyalty — Cloud Storage security rules
//
// Nothing here is client-accessible. Open access deliberately, per path, if and when a feature needs it.

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
